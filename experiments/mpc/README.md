# MPC Experiments

This folder is for experimenting with Model Predictive Control (MPC) controllers.

## Run (Pure MPC Controller)

From the repo root:

```powershell
python experiments/mpc/run_pure_mpc.py
```

Outputs:
 - Per-patient logs in `results/`
 - Summary CSV in `results/`

